<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.library.mapper.CategoryMapper">
    
    <resultMap id="CategoryResultMap" type="com.example.library.entity.Category">
        <id property="id" column="id"/>
        <result property="name" column="name"/>
        <result property="description" column="description"/>
        <result property="parentId" column="parent_id"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>
    
    <select id="findAll" resultMap="CategoryResultMap">
        SELECT * FROM category
    </select>
    
    <select id="findById" parameterType="int" resultMap="CategoryResultMap">
        SELECT * FROM category WHERE id = #{id}
    </select>
    
    <insert id="insert" parameterType="com.example.library.entity.Category" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO category (name, description, parent_id, create_time, update_time)
        VALUES (#{name}, #{description}, #{parentId}, NOW(), NOW())
    </insert>
    
    <update id="update" parameterType="com.example.library.entity.Category">
        UPDATE category
        SET name = #{name},
            description = #{description},
            parent_id = #{parentId},
            update_time = NOW()
        WHERE id = #{id}
    </update>
    
    <delete id="deleteById" parameterType="int">
        DELETE FROM category WHERE id = #{id}
    </delete>

    <select id="findChildIds" resultType="int">
        SELECT id FROM category WHERE parent_id = #{parentId}
    </select>
</mapper>